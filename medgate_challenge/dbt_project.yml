
name: 'medgate_challenge'
version: '1.8.8'

profile: 'medgate_challenge'


model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:         # directories to be removed by `dbt clean`
  - "target"
  - "dbt_packages"


models:
  medgate_challenge:
    staging:
      +materialized: view
      +schema: staging
    mart:
      +materialized: table
      +schema: master
